# Copy this file to ".env" and fill in real values. Never commit the real .env file.

# Local dev default is SQLite (no setup needed). Switch to Postgres later for deployment:
# DATABASE_URL=postgresql://username:password@localhost:5432/skiniq
DATABASE_URL=sqlite:///./skiniq.db

SECRET_KEY=changeme_generate_a_real_random_string_later
GOOGLE_PLACES_API_KEY=your_key_here_for_dermatologist_locator_later
